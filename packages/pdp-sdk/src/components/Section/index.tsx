import { styled } from '@nykaa/ui-components';

export default styled.section`
  padding: ${({ theme }) => theme.spacing.spacing80};
  border-bottom: 4px solid ${({ theme }) => theme.colors.snow300};
`;
